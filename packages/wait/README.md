# Wait

Returns a promise that resolves after specified milliseconds

# Usage

```typescript
import wait from "@node-api-toolkit/wait";

(async () => {
  // waits a second
  await wait(1000);
})();
```
